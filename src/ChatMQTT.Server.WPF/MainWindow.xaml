<Window x:Class="ChatMQTT.Server.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatMQTT.Server.WPF"
        mc:Ignorable="d"
        Title="MQTT Broker Server" Height="700" Width="1000" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Server Control Panel -->
        <Border Grid.Row="0" Background="#34495E" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Port:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
                <TextBox Grid.Column="1" x:Name="txtPort" Text="1883" VerticalAlignment="Center" Margin="0,0,20,0" Padding="8" FontSize="14"/>

                <TextBlock Grid.Column="2" Text="Server Status:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
                <TextBlock Grid.Column="3" x:Name="txtServerStatus" Text="Stopped" Foreground="Red" VerticalAlignment="Center" FontWeight="Bold" FontSize="14"/>

                <Button Grid.Column="4" x:Name="btnStartStop" Content="Start Server" Click="BtnStartStop_Click"
                        Background="#27AE60" Foreground="White" Padding="20,8" FontWeight="Bold" FontSize="14" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Statistics Panel -->
        <Border Grid.Row="1" Background="#ECF0F1" Padding="15" BorderBrush="#BDC3C7" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Connected Clients" FontWeight="Bold" FontSize="12" Foreground="#7F8C8D"/>
                    <TextBlock x:Name="txtConnectedClients" Text="0" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="Total Messages" FontWeight="Bold" FontSize="12" Foreground="#7F8C8D"/>
                    <TextBlock x:Name="txtTotalMessages" Text="0" FontSize="28" FontWeight="Bold" Foreground="#3498DB"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="Active Topics" FontWeight="Bold" FontSize="12" Foreground="#7F8C8D"/>
                    <TextBlock x:Name="txtActiveTopics" Text="0" FontSize="28" FontWeight="Bold" Foreground="#9B59B6"/>
                </StackPanel>

                <StackPanel Grid.Column="3">
                    <TextBlock Text="Uptime" FontWeight="Bold" FontSize="12" Foreground="#7F8C8D"/>
                    <TextBlock x:Name="txtUptime" Text="00:00:00" FontSize="28" FontWeight="Bold" Foreground="#E74C3C"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Connected Clients Panel -->
            <Border Grid.Column="0" BorderBrush="#BDC3C7" BorderThickness="1" Margin="0,0,5,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#3498DB" Padding="10">
                        <TextBlock Text="Connected Clients" Foreground="White" FontWeight="Bold" FontSize="16"/>
                    </Border>

                    <DataGrid Grid.Row="1" x:Name="dgClients" AutoGenerateColumns="False"
                              IsReadOnly="True" GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column" Background="White"
                              RowBackground="White" AlternatingRowBackground="#F8F9FA">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Client ID" Binding="{Binding ClientId}" Width="*"/>
                            <DataGridTextColumn Header="Connected At" Binding="{Binding ConnectedAt, StringFormat='{}{0:HH:mm:ss}'}" Width="100"/>
                            <DataGridTextColumn Header="Messages" Binding="{Binding MessagesSent}" Width="80"/>
                            <DataGridTextColumn Header="Endpoint" Binding="{Binding Endpoint}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Activity Log Panel -->
            <Border Grid.Column="1" BorderBrush="#BDC3C7" BorderThickness="1" Margin="5,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#E74C3C" Padding="10">
                        <Grid>
                            <TextBlock Text="Activity Log" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                            <Button x:Name="btnClearLog" Content="Clear" Click="BtnClearLog_Click"
                                    HorizontalAlignment="Right" Background="White" Foreground="#E74C3C"
                                    Padding="10,5" FontWeight="Bold" Cursor="Hand"/>
                        </Grid>
                    </Border>

                    <ScrollViewer Grid.Row="1" x:Name="scrollViewerLog" VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="txtLog" Padding="10" FontFamily="Consolas" FontSize="12"
                                   Background="White" TextWrapping="Wrap"/>
                    </ScrollViewer>

                    <Border Grid.Row="2" Background="#F8F9FA" Padding="10" BorderBrush="#BDC3C7" BorderThickness="0,1,0,0">
                        <StackPanel>
                            <TextBlock Text="Topic Subscriptions" FontWeight="Bold" Margin="0,0,0,5"/>
                            <ItemsControl x:Name="lstTopics">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,2" Padding="5" Background="#ECF0F1" CornerRadius="3">
                                            <TextBlock>
                                                <Run Text="{Binding Topic}" FontWeight="Bold"/>
                                                <Run Text=" (" Foreground="#7F8C8D"/>
                                                <Run Text="{Binding SubscriberCount}" Foreground="#7F8C8D"/>
                                                <Run Text=" subscribers)" Foreground="#7F8C8D"/>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
